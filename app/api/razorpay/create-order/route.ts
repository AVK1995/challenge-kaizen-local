  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

K  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

F  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

G  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

@  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

A  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

A  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

M  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

J  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

>  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

?  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

>  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

?  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

q  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

q  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

K  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

F  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

G  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

[  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

]  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

3  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

<  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

>  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

q  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

4  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

8  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

8  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

\  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

D  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

8  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

L  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

3  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

4  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

?  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

?  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

<  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

>  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

[  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

]  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

<  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

>  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

4  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

4  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

q  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

L  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

M  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

q  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

*  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

$  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

K  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

F  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

G  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

v  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

1  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

B  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

$  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

B  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

$  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

$  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

6  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

4  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

J  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

S  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

K  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

F  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

G  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

P  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

H  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

E  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

K  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

U  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

T  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

C  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

O  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

F  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

G  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

_  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

$  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

D  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

`  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

=  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

|  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

!  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

?  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

[  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

]  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

z  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

m  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

I  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

/  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

y  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

g  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

b  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

h  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

[  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

c  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

-  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

]  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

i  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

d  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

N  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

x  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

R  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

p  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

.  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

j  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

(  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

f  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

l  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

n  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

e  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

w  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

o  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

r  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

k  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

'  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

,  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

{  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

a  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

t  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

u  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

s  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

:  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

5  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

0  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

2  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

)  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

;  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

   const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

}  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };


  const utm = (body.utm ?? {}) as Record<string, string | undefined>;

  /* ⚠️ RAZORPAY CAPS notes AT 15 KEY-VALUE PAIRS. This block is at 14, so
     there is exactly one slot spare: adding two more fields silently costs you
     the order, not just the note.

     Two things were folded to make room. `name` is gone, because firstName and
     lastName now arrive separately and a joined copy is redundant. The three
     utm_* keys are packed into one pipe-delimited `utm`, unpacked in the
     webhook. */
  const notes: Record<string, string> = {
    kind: 'kaizen_5day_reset',
    firstName: truncate(firstName),
    lastName: truncate(lastName),
    email: truncate(email),
    phone: truncate(phone),
    city: truncate(city),
    country: truncate(country),
    occupation: truncate(occupation),
    externalId: truncate(body.externalId, 64),
    fbc: truncate(body.fbc),
    fbp: truncate(body.fbp),
    gaCid: truncate(body.gaClientId, 64),
    /* The bare checkout URL, not the full one: query strings carry fbclid, and
       event_source_url is metadata for Meta rather than a matching signal, so
       trimming it costs no match quality. */
    esu: `${CHECKOUT_CONFIG.fallbackEventSourceUrl}/checkout`,
    utm: truncate(
      [utm.source ?? '', utm.medium ?? '', utm.campaign ?? ''].join('|'),
      250,
    ),
  };

